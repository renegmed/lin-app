import React from "react";

import NavigationItems from "./NavigationItems";

const toolbar = props => (
  <header>
    <nav>
      <NavigationItems />
    </nav>
  </header>
);

export default toolbar;
